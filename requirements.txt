# XEF2JPEG Dependencies

# Core dependencies for Kinect v2 XEF file handling
# Note: Kinect for Windows SDK 2.0 must be installed separately on Windows

# GUI framework (included with Python)
# tkinter is included with Python standard library

# Image processing
Pillow>=10.0.0

# Numerical processing (used for depth/IR frame data)
numpy>=1.24.0

# For potential future enhancements
# opencv-python>=4.8.0
